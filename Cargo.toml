[package]
name = "spaceship"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", features = ["wayland"] }
rand = { version = "0.8.5", features = ["small_rng"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[features]
bevy_debug_stepping = []
